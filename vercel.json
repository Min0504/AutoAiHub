{
  "version": 2,
  "buildCommand": "vite build && esbuild api/index.src.ts --bundle --platform=node --format=cjs --outfile=api/index.js",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/sitemap.xml", "destination": "/api/index" }
  ]
}
